# Clawster API Configuration

# Database
DATABASE_URL=file:./dev.db

# Authentication - REQUIRED: Generate a secure secret (min 32 chars)
# Generate with: openssl rand -base64 48
JWT_SECRET=

# Server
PORT=4000

# Default deployment target (docker, local, ecs-ec2, gce, azure-vm)
DEFAULT_DEPLOYMENT_TARGET=docker

# Credential Encryption - REQUIRED in production (64 hex chars)
# Generate with: openssl rand -hex 32
CREDENTIAL_ENCRYPTION_KEY=

# Frontend URL for CORS (WebSocket connections)
FRONTEND_URL=http://localhost:3000

# AWS Configuration (for ECS EC2 deployment)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Azure Configuration (for ACI deployment)
# AZURE_SUBSCRIPTION_ID=
# AZURE_RESOURCE_GROUP=
# AZURE_REGION=eastus
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=
# AZURE_KEY_VAULT_NAME=
# AZURE_LOG_ANALYTICS_WORKSPACE_ID=
